import { Attribute } from "../type/attribute";

export const RedCore = (): Attribute => ({
  name: "Weapon Damage",
  amplifier: "WDCore",
  type: "Attribute",
  note: "Red Core",
  uptime: 1,
  value: 0.15,
});
